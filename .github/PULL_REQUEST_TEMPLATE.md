## What changed

## Why

## How was this tested

- [ ] `npm test` passes in `server/` and `client/`
- [ ] `docker compose build` succeeds locally
- [ ] Verified the change in a running container where applicable
